# User-generated / local artifacts -- do NOT commit.
# mTLS certs (incl. private keys) are generated per conf/README.md:
conf/certs/
# the SoapySDR zmqrx bridge is built by the user outside the repo (see README):
soapy-zmq-bridge/
*.log
*.crash
conf/*.state
__pycache__/
# Regenerated demo media (DASH/HLS segments dropped by the encoder into the watchfolder for the
# BM-SC to pick up) -- large, transient, rebuilt every run. Not source, do NOT commit.
demo-content/watchfolder/
